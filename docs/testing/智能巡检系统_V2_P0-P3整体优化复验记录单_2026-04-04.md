# 智能巡检系统 V2 P0-P3 整体优化复验记录单

## 1. 复验对象

- 复验标题：V2 治理收敛与架构解耦优化（P0-P3）
- 对应提审单：`docs/testing/智能巡检系统_V2_P0-P3整体优化提审单_2026-04-04.md`
- 当前分支 / `git_sha`：`codex/job-schedules-v2` / `c18a852`
- 复验日期：2026-04-04
- 复验人：`待填写`

## 2. 自动化复验证据

| 项目 | 命令 | 结果 | 备注 |
|---|---|---|---|
| 后端全量 | `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest backend-v2/tests -q` | `126 passed in 64.14s` | 含 job/scheduler/alert/celery/runtime 回归 |
| 前端全量 | `npm test` | `5 passed, 17 tests passed` | 当前环境有 localStorage warning，不影响结论 |
| 前端构建 | `npm run build` | 通过 | `tsc -b && vite build` 成功 |
| 门禁契约 | `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest backend-v2/tests/test_release_checklist_report.py backend-v2/tests/test_release_drill_report.py backend-v2/tests/test_v2_script_contracts.py -q` | `16 passed in 4.66s` | 覆盖 release checklist / drill / script contract |
| Shell 语法 | `for f in scripts/v2/*.sh; do bash -n "$f"; done` | `OK` | 全部 shell 脚本语法通过 |

## 3. 手工复验清单

### 3.1 基线与治理

- [ ] 需求、计划、README、验收清单中的技术基线一致
- [ ] 提审材料、追踪矩阵、治理规范可从 `docs/INDEX.md` 正常定位
- [ ] release checklist / gate 不再依赖“最新文件”

### 3.2 后端边界

- [ ] `job_service` 不再直接 import worker task
- [ ] `scheduler_service` 不再直接 import worker task
- [ ] `alert_service` 作为 facade 仍兼容原路由和测试 monkeypatch 用法

### 3.3 前端边界

- [ ] 菜单可见性与页面可访问性一致
- [ ] `@/shared/api/configCenter` 现有调用不需要改路径
- [ ] 构建产物中 `configCenter` 仅表现为兼容层，不引入额外断裂

### 3.4 发布门禁

- [ ] `summary.json` / `gate-summary.json` 含 `run_id`、`git_sha`、`branch`、`started_at`、`finished_at`、`environment`
- [ ] `release-gate` 绕过项要求显式 `--override-reason`
- [ ] `verify` 缺失前置摘要时应失败

## 4. 复验结论

- 复验结果：`待填写`
- 是否允许进入发布门禁：`待填写`
- 阻断项：`待填写`
- 备注：`待填写`

## 5. 关闭信息

- 缺陷是否全部进入 `Retest -> Closed / Rejected`：`待填写`
- 复验证据路径：`docs/testing/智能巡检系统_V2_P0-P3整体优化复验记录单_2026-04-04.md`
- 责任人确认：`待填写`
