# 智能巡检系统 V2 治理状态补充说明

## 1. 文档目的

本文档用于补充 `2026-04-04` 批次提审、发布说明和复验记录之后的当前治理状态，避免继续把当时的待办项、未勾选项和阶段性判断误读为 `2026-04-12` 的当前事实。

`2026-04-04` 的提审/发布/复验文档仍保留为历史过程记录；当前代码结构、治理边界和发布口径，请结合以下两份文档共同阅读：

- `docs/architecture/智能巡检系统_V2_当前代码事实与治理边界说明.md`
- `docs/testing/智能巡检系统_V2_治理状态补充说明_2026-04-12.md`
- `docs/testing/智能巡检系统_V2_治理收敛提审单_2026-04-12.md`
- `docs/testing/智能巡检系统_V2_治理收敛复验记录单_2026-04-12.md`

## 2. 本次补充覆盖范围

截至 `2026-04-12`，本补充只确认以下治理收敛项：

1. 后端服务边界回归保护
2. 真实异步链路 readiness 与预检基线
3. 默认发布门禁口径
4. 旧数据回灌与 release baseline 依赖澄清
5. README / 索引 / 历史分析文档的当前事实提示

本补充不代表全仓库所有历史优化项都已再次全量复验；未在本文档列出的事项，仍需以对应脚本、测试和最新产物为准。

## 3. 当前已确认的状态

### 3.1 后端边界

- `job_service` 与 `scheduler_service` 当前仅作为兼容 facade 存在，不再承载新增业务判断。
- `app/schedulers/runner.py` 当前直接组合以下拆分服务：
  - `schedule_dispatch_service`
  - `camera_status_sweep_service`
  - `signal_monitor_orchestrator`
  - `alert_delivery_service`
- `task_dispatcher` 仍是服务层进入异步 worker 调度的唯一边界。
- `backend-v2/tests/test_service_module_boundaries.py` 已新增边界锁定测试，明确禁止：
  - API routes 回退为直接依赖 `job_service` / `scheduler_service`
  - `runner.py` 回退为依赖 `scheduler_service`
  - facade 模块重新长出非 re-export 业务逻辑

### 3.2 预检与真实异步链路

- `./scripts/v2/preflight.sh --with-e2e` 已于 `2026-04-12` 再次通过。
- 最新预检产物：
  - `data/preflight-logs/preflight-20260412-222322-49f760fc/summary.json`
- 当前预检摘要已显式包含：
  - `run_id`
  - `git_sha`
  - `branch`
  - `git_dirty`
  - `started_at`
  - `finished_at`
  - `environment`
  - API / worker / scheduler readiness 状态
- 当前通过结果对应：
  - API readiness: `ready`
  - worker readiness: `ready`
  - scheduler readiness: `ready`
  - smoke / perf / soak / e2e: `passed`

### 3.3 发布门禁

- 默认发布入口仍为 `make v2-release-gate-final`。
- 最新一次无 override 的发布门禁已通过：
  - `data/release-gates/release-gate-20260412-213844-49f760fc/gate-summary.json`
- 当前 gate 摘要确认：
  - `ready_to_release: true`
  - `override.active: false`
  - `release_policy.default_entrypoint: make v2-release-gate-final`
  - `release_policy.override_reason_required: true`
  - `release_policy.bypass_run: false`

### 3.4 旧数据与回灌基线

- `data/surveillance.db` 与 `data/detections/` 下的指定历史样本文件共同构成当前 `reconcile` / `release-drill` 的事实基线。
- 已确认至少有 13 个历史样本文件属于发布门禁必需 fixture，不能再按“历史垃圾文件”直接清理。
- 这组依赖已经由以下手段保护：
  - `.gitignore` 显式放行
  - `backend-v2/tests/test_v2_script_contracts.py` 完整性测试
  - 当前代码事实文档中的基线说明

### 3.5 文档与入口

- `README_V2.md` 已明确“当前代码事实”文档优先于历史综合分析结论。
- `README_V2.md` 的手工启动顺序已与实际脚本保持一致，依赖服务包括 `local-detector`。
- `docs/architecture/智能巡检系统_V2_终版综合分析报告.md` 已在文首标记为历史分析参考，不再直接代表当前代码事实。
- 前端已移除本轮 `antd` 运行时弃用告警对应的源码用法，并由 `frontend-v2/src/test/antdDeprecatedProps.test.ts` 静态约束测试保护。

## 4. 对 2026-04-04 历史文档的解释方式

以下文档应视为当时的过程记录，而不是当前状态快照：

- `docs/testing/智能巡检系统_V2_P0-P3整体优化提审单_2026-04-04.md`
- `docs/testing/智能巡检系统_V2_P0-P3整体优化发布说明_2026-04-04.md`
- `docs/testing/智能巡检系统_V2_P0-P3整体优化复验记录单_2026-04-04.md`

因此，若其中出现以下内容，不应直接当成当前事实：

1. `待填写`、`待复验`、未勾选复验项
2. 早于 `2026-04-12` 的 `git_sha`、分支、测试结果
3. 尚未反映 `release-gate-final` 与最新 preflight 产物的说明
4. 尚未反映 facade 已退化为兼容层、边界测试已补齐的状态

## 5. 已完成归档与剩余跟进项

截至 `2026-04-12`，以下归档项已完成：

1. 本轮治理签字材料已单独归档为 `2026-04-12` 批次文档，而未覆盖 `2026-04-04` 的历史记录：
   - `docs/testing/智能巡检系统_V2_治理收敛提审单_2026-04-12.md`
   - `docs/testing/智能巡检系统_V2_治理收敛复验记录单_2026-04-12.md`

以下事项仍建议作为后续治理批次继续推进：

1. 若后续继续升级 `antd`，建议把 `frontend-v2/src/test/antdDeprecatedProps.test.ts` 的覆盖范围扩展到更多 deprecated API，而不只限于本轮已清理的 `Space.direction`、`Alert.message`、`Drawer.width`。
