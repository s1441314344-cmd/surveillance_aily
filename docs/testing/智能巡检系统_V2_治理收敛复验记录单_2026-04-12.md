# 智能巡检系统 V2 治理收敛复验记录单

> 说明：本文档对应 `2026-04-12` 时点的自动化复验与当前事实核对结果；人工签字信息仍需由项目负责人补齐。

## 1. 复验对象

- 复验标题：V2 治理收敛与发布基线固化（2026-04-12）
- 对应提审单：`docs/testing/智能巡检系统_V2_治理收敛提审单_2026-04-12.md`
- 当前分支 / `git_sha`：`codex/job-schedules-v2` / `49f760fc0752425cc9a9bb1d58cb5395d557711f`
- 复验日期：2026-04-12
- 复验人：`待填写`

## 2. 自动化复验证据

| 项目 | 命令 / 产物 | 结果 | 备注 |
|---|---|---|---|
| 后端边界与脚本契约 | `pytest backend-v2/tests/test_service_module_boundaries.py backend-v2/tests/test_task_dispatch_boundaries.py backend-v2/tests/test_v2_script_contracts.py backend-v2/tests/test_provider_adapters.py -q` | `31 passed in 9.21s` | 覆盖 facade 边界、dispatcher 边界、legacy fixture 契约、provider/schema 回归 |
| 前端单测 | `npm test` | `7 passed, 21 tests passed` | 含 `frontend-v2/src/test/antdDeprecatedProps.test.ts`，用于阻止 `Space.direction` / `Alert.message` / `Drawer.width` 回退 |
| 真实异步预检 | `./scripts/v2/preflight.sh --with-e2e` | 通过 | `summary.json` 显示 API / worker / scheduler readiness 全部 `ready`，且 `e2e: passed` |
| UAT 汇总 | `data/release-gates/release-gate-20260412-213844-49f760fc/uat/summary.json` | 通过 | `backend_pytest`、`frontend_lint`、`frontend_unit`、`frontend_build`、`e2e`、`security`、`reconcile`、`release_drill` 全部 `passed` |
| 默认发布门禁 | `make v2-release-gate-final` | 通过 | `gate-summary.json` 中 `ready_to_release: true`，且无 override |

## 3. 手工复验清单

### 3.1 当前事实源

- [x] `README_V2.md` 已明确“当前代码事实”文档优先于历史综合分析结论
- [x] `docs/INDEX.md` 可定位到 `2026-04-12` 的治理状态补充说明
- [x] 历史综合分析文档已标记为历史参考，不再直接代表当前代码事实

### 3.2 后端边界

- [x] `job_service` / `scheduler_service` 当前仅作为兼容 facade 存在
- [x] API routes 未回退为依赖 facade
- [x] `app/schedulers/runner.py` 当前直接组合拆分后的调度服务
- [x] 服务层直接 import worker task 的边界仍只保留在 `task_dispatcher`

### 3.3 发布基线与证据链

- [x] `preflight` 摘要包含 `run_id`、`git_sha`、`branch`、`git_dirty`、`started_at`、`finished_at`、`environment`
- [x] API / worker / scheduler 都有显式 readiness 证据
- [x] `make v2-release-gate-final` 仍为默认发布入口，最新通过记录没有 override / bypass
- [x] `reconcile` / `release-drill` 的历史样本基线依赖已被文档与测试同时保护
- [x] 本轮已观测到的 `antd` 运行时弃用告警来源已从源码移除，并有静态测试保护

### 3.4 前端与构建口径

- [x] 前端 lint / unit / build / E2E 已在最新 UAT 汇总中全部通过
- [x] `README_V2.md` 的依赖启动顺序已与实际脚本保持一致，包含 `local-detector`

## 4. 复验结论

- 复验结果：`通过（自动化复验通过，人工签字待补）`
- 是否允许进入发布门禁：`是`
- 阻断项：`无新增阻断项`
- 备注：
  - 最新预检证据：`data/preflight-logs/preflight-20260412-223503-49f760fc/summary.json`
  - 最新发布门禁证据：`data/release-gates/release-gate-20260412-213844-49f760fc/gate-summary.json`
  - 当前仍建议未来继续扩展 `antd` deprecated API 的静态约束范围，但本轮已观测到的三类运行时告警已清理

## 5. 关闭信息

- 缺陷是否全部进入 `Retest -> Closed / Rejected`：`待项目系统同步；当前仓库内自动化证据已满足复验前提`
- 复验证据路径：
  - `docs/testing/智能巡检系统_V2_治理收敛复验记录单_2026-04-12.md`
  - `docs/testing/智能巡检系统_V2_治理状态补充说明_2026-04-12.md`
- 责任人确认：`待填写`
