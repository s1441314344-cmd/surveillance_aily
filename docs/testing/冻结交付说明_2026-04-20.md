# 冻结交付说明（2026-04-20）

## 交付范围

本次交付进入冻结模式，不再新增重构，目标是把当前“PRD + 代码 + 验证”基线打包给研发与评审。

## PRD 基线

- 总览：`/Users/shaopeng/Downloads/surveillance_aily/prd/需求总览.md`
- 模块：`/Users/shaopeng/Downloads/surveillance_aily/prd/modules/*.md`（17 份）

## 验证结论

- 前端：
  - `npm test` 通过（`66 files / 168 tests passed`）
  - `npm run lint -- --max-warnings=0` 通过
  - `npm run build` 通过
  - `npm run e2e:mainline` 通过（`5 passed`）
- 后端：
  - `pytest` 通过（`192 passed`）

## 冒烟脚本说明

`make v2-smoke` 受运行环境影响（API/worker/scheduler 是否在线、API 基址是否一致）。本次已在独立运行栈完成实跑。  
本次执行命令：

```bash
cd /Users/shaopeng/Downloads/surveillance_aily
V2_API_BASE_URL=http://127.0.0.1:5801 make v2-smoke
```

执行结果（2026-04-20）：

- `v2-smoke` 成功
- 关键阶段全部通过：
  - `invalid upload guard ok`
  - `upload async flow ok`
  - `failed->retry async flow ok`
  - `scheduled async flow ok`

## 本轮冻结前最小修复

- 文件：`/Users/shaopeng/Downloads/surveillance_aily/frontend-v2/e2e/auth-and-navigation.spec.ts`
- 修复：菜单文案断言 `模型与设置` -> `模型与系统设置`
- 目的：修复 E2E 与当前导航事实不一致导致的假失败

## 交付结论

- PRD 拆分与回写：完成
- 路由/权限与 PRD 对齐：完成
- 前后端主门禁：完成
- 最小 E2E：完成
- 冒烟脚本：待在目标环境执行
